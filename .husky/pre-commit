#!/bin/sh
if git diff --cached --name-only | grep -q ".env"; then
  echo "❌ SECURITY ALERT: You are attempting to commit a .env file!"
  echo "Please remove it from staging (git reset .env) and add it to .gitignore."
  exit 1
fi
